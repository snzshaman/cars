# cars
- 4 класса: `BaseCar` : `Car`, `Truck`, `SpecMachine` 
- Читает `cars.csv`, пропускает косяки, остальное складывает в `list[BaseCar]`
- `Truck` сам парсит размеры кузова "2.5x3x2" → свойства `body_length`, `body_width`, `body_height` + объём
